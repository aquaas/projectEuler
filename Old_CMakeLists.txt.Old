cmake_minimum_required (VERSION 2.6)
project (pe001)

#make VERBOSE=1

set (pe001_VERSION_MAJOR 1)
set (pe001_VERSION_MINOR 0)

get_filename_component(PARENT_DIR ${CMAKE_SOURCE_DIR} PATH)

add_executable(pe001 pe001.cpp)

INCLUDE_DIRECTORIES(${PARENT_DIR}/libs/primesieve)
LINK_DIRECTORIES(${PARENT_DIR}/libs/primesieve)

TARGET_LINK_LIBRARIES(pe001 primesieve)




project (pe003)

add_executable(pe003 pe003.cpp)
INCLUDE_DIRECTORIES(${PARENT_DIR}/libs/primesieve)
LINK_DIRECTORIES(${PARENT_DIR}/libs/primesieve)

TARGET_LINK_LIBRARIES(pe003 primesieve)


set (CMAKE_CXX_FLAGS "-std=c++11 -Wall -g")


